{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Build MyApp",
            "command": "make",
            "args": ["all"],
            "options": {
                "cwd": "${cwd}"
            },
            "detail": "Build the app (MyApp) using the makefile"
        }     
    ],
    "version": "2.0.0"
}